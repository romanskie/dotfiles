[user]
	name = Roman Schader
	email = me@rschader.com
	user = Roman Schader
[core]
	editor = code --wait
[alias]
	plog = log --graph --pretty=format:'%h -%d %s %n' --abbrev-commit --date=relative --branches
[merge]
	tool = vimdiff
	conflictstyle = diff3
[mergetool]
	prompt = false
